fn factorial(n) -> f {
    1 -> f;
    if (n > 1) {
        factorial(n - 1) -> result;        
        n * result -> f;
    }
}

start {
    arg 0 -> N;
    factorial(N) -> result;
    pr "result: "; prn result;
}
